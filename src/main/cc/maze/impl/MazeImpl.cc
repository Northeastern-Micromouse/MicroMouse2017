#include "MazeImpl.h"

MazeImpl::MazeImpl() {
	maze_.resize(256);
}

std::vector<Cell *> MazeImpl::GetNeighbors(int row, int col, std::vector<Cell::Direction> neighbors) {
	// TODO(matt): Implement
	return NULL;
}

